# Machine_Learning_Kmeans_Clustering
We load in a genome dataset that contains 400 genomes and 2 properties each. We use machine learning to figure out properties about the genomes via k-means clustering.
